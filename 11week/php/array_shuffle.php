<?php
    // 로또 발생 구문
    // 배열에 45까지 입력
    $arr = array();
	for ($i = 0; $i < 45; $i++) {
        $arr[i] = (i+1);
    }

    // 무작위로 섞기
    shuffle($arr);

    // 맨 앞에서부터 6개의 숫자 뽑아내기

    // 점수 출력
    echo ("[로또 추첨 결과]<br>");
    

?>